# ============================================================
# midnight-fe — Vite frontend env
# 複製為 .env.local（本機）；production 由 GitHub Actions 在 build 時注入
# Vite 規則：必須 VITE_ 開頭才會 expose 到 client bundle
# 注意：env 在 build 時嵌入，改值需重新 build
# ============================================================

# Directus API base URL（含 protocol，不含尾斜線）
# - 本機開發：http://localhost:8055
# - production：https://mmoodvie.live （經由 nginx 反代到 :8055）
VITE_DIRECTUS_URL=http://localhost:8055

# TMDB API key（可選，用於 Result 頁直接列出台灣串流平台）
# - 在 https://www.themoviedb.org/settings/api 申請（v3 auth），複製 "API Key"
# - 留空時 Result 頁會 fallback 回舊的「哪裡看 ↗」JustWatch 連結
# - 為 read-only public key，bundle 進前端無安全風險
VITE_TMDB_API_KEY=

# PostHog 分析（可選）
# - 留空 = 不載入 PostHog（dev 預設）
# - production：phc_... 開頭的 Project API Key，從 app.posthog.com → Project settings 取得
# - HOST 預設 US region；若 PostHog 帳號建在 EU 改用 https://eu.i.posthog.com
